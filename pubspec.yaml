name: polla
description: Polling App

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=2.10.5 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.0
  firebase_core: ^0.7.0
  firebase_auth: ^0.20.0+1
  google_sign_in: ^4.5.9

dev_dependencies:
  flutter_test:
    sdk: flutter
#  integration_test:
#    sdk: flutter

flutter:
  uses-material-design: true
  fonts:
    - family: ArimaMadurai
      fonts:
        - asset: fonts/ArimaMadurai-Black.ttf
          weight: 900

  assets:
    - images/
